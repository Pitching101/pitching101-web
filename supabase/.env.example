# Copy to supabase/.env.local for local `supabase functions serve`.
# supabase/.env.local stays uncommitted. These are not Vercel variables.
RESEND_API_KEY=
WEBHOOK_SECRET=
# Optional. Defaults to: Pitching101 <onboarding@resend.dev>
# RESEND_FROM=
